# REFLECTION.md 学生修改指南

> 课程规定反思必须由学生本人撰写。本文件只列事实校正和结构建议，不提供替换正文。

## 必须修改

1. 当前约 2696 个汉字，需删至 1500–2500 字；建议目标 2100–2300 字，给最后调整留余量。
2. 删除“完整产品部署在公网”“选择阿里云服务器”的现状陈述。最终架构是：完整产品仅本地 loopback；线上只发布无服务器、无 Key 的静态机制演示。
3. 将“加密文件配主密码”改为本人对当前方案的真实理解：持久 Key 使用 OS 凭据库，临时 Key 只在内存；不要写任何真实 Key。
4. 更新测试事实：Core 31 files/297 tests；Server 23 files/197 tests；CLI 安装生命周期 4/4。不要用无法定义口径的“450+ 测试”。
5. 不要声称“每次变动都及时上传 GitHub”。应只描述能从 commit/PR/CI 历史核验的操作。
6. 修正过时说法：CLI 自动 clean install/start/WebUI/cleanup 已闭合；产物导出现在有一小时窗口并长期保存 manifest 副本。

## 推荐结构（由学生用自己的话填写）

1. 项目与本人决策：为什么选择治理作为重点，为什么完整产品不部署服务器。
2. Superpowers 评价：brainstorming、writing-plans、TDD、review、worktree 各自带来的真实收益与形式成本。
3. 一个成功案例：反馈测试如何证明“因果”，而不只是预排两个 mock 响应。
4. 一个失败案例：本地预览发现“写出了文件但未登记产物”“完成会话被立即回收”；说明为什么单元测试绿色仍需要端到端预览。
5. 凭据与分发：OS keyring、memory-only、npm clean install 分别迫使本人考虑了什么。
6. 批判与重做：task 粒度、阶段审查、真实用户路径测试、AI 建议何时应被推翻。

## 建议删除或压缩

- 泛泛介绍 AI 很强、未来会改变世界的段落可大幅压缩。
- 重复解释 brainstorming 和 subagent 的段落合并。
- 未有仓库证据的“62 个 task”“两轮发现八个问题”等精确数字，若无法提供出处，改为本人能确认的定性描述。
- 删除与最终方案无关的服务器选型和部署权限细节。

## 文字修正

- “课程的的内容”→“课程的内容”
- “阶段间相互以来”→“阶段间相互依赖”
- “代码原件”→“实现代码”
- 数组“版权”→“所有权”
- “完成这个项目的工程中”→“完成这个项目的过程中”
- 统一 `Superpowers`、`Agent`、`Subagent`、`MockLLMAdapter`、`SPEC`、`PLAN`、`TDD`

## 提交前自查

- 汉字数在 1500–2500。
- 所有第一人称判断确实是本人经历和观点。
- 没有真实凭据、虚构部署、过期测试数字或无法核验的外部操作。
- 若使用 AI 辅助润色，按课程要求如实标注辅助范围。
